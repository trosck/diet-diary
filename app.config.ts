export default defineAppConfig({
  ui: {
    button: {
      base: "capitalize",
      default: {
        size: "xl",
      },
    },
    input: {
      default: {
        size: "xl",
      },
    },
    select: {
      default: {
        size: "xl",
      },
    },
  },
});
