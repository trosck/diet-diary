export default defineAppConfig({
  ui: {
    button: {
      default: {
        size: "xl",
      },
    },
    input: {
      default: {
        size: "xl",
      },
    },
    select: {
      default: {
        size: "xl",
      },
    },
  },
});
